<Window x:Class="Bend.SourceControl.CommitMessageDialog"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Commit Changes" Width="500" SizeToContent="Height" ResizeMode="NoResize"
 WindowStyle="None" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
 AllowsTransparency="True" Background="Transparent" KeyDown="Window_KeyDown">
 <Window.Resources>
  <Style x:Key="DialogButton" TargetType="Button">
   <Setter Property="Width" Value="82"/><Setter Property="Height" Value="24"/>
   <Setter Property="FontSize" Value="10"/><Setter Property="FontWeight" Value="Bold"/>
   <Setter Property="FontFamily" Value="Segoe UI"/>
  </Style>
 </Window.Resources>
 <Border>
  <Border.BitmapEffect>
   <DropShadowBitmapEffect Color="Black" Direction="320" ShadowDepth="5" Softness="1" Opacity="0.5"/>
  </Border.BitmapEffect>
  <Grid Margin="20" Background="{DynamicResource BackgroundBrush}">
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="110"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <Grid Margin="0,20,0,10">
    <Grid.ColumnDefinitions><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border Background="{DynamicResource LogoBackgroundBrush}"/>
    <TextBlock Grid.Column="1" Text="COMMIT STAGED CHANGES" Margin="20,0,10,0" FontSize="16" FontWeight="ExtraLight" FontFamily="Segoe UI" VerticalAlignment="Center" Foreground="{DynamicResource ForegroundBrush}"/>
   </Grid>
   <TextBox x:Name="MessageBox" Grid.Row="1" Margin="30,0,10,0" AcceptsReturn="True" TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" Padding="7" FontFamily="Segoe UI" FontSize="13"
            Foreground="{DynamicResource ForegroundBrush}" Background="{DynamicResource ShellChromeBrush}"
            BorderBrush="{DynamicResource ShellBorderBrush}" CaretBrush="{DynamicResource ForegroundBrush}"
            TextChanged="MessageBox_TextChanged"/>
   <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,25,12">
    <Button x:Name="CommitButton" Content="COMMIT" Style="{StaticResource DialogButton}" IsDefault="True" IsEnabled="False" Click="Commit_Click"/>
    <Button Content="CANCEL" Style="{StaticResource DialogButton}" IsCancel="True" Margin="10,0,0,0"/>
   </StackPanel>
  </Grid>
 </Border>
</Window>
